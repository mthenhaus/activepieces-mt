import { MailData, MailAddress } from '../../transport/mail/types.js';
import { SendMailParams } from './sendMailParams.js';

/**
 * Temporary adapter migration function to convert from SDK mail params to internal params.
 *
 * @deprecated This will be removed in favor of removing the internal params and accepting these params.
 */
declare function toMailData(params: SendMailParams): MailData;
/**
 * Temporary adapter migration function to convert from internal MailData to SDK mail params.
 *
 * @deprecated This will be removed in favor of removing the internal params and accepting these params.
 */
declare function createMailAddress(address: string): MailAddress;
/**
 * Temporary adapter migration function to convert from internal MailData to SDK mail params.
 *
 * @deprecated This will be removed in favor of removing the internal params and accepting these params.
 */
declare function fromMailData(mailData: MailData): SendMailParams;

export { createMailAddress, fromMailData, toMailData };
