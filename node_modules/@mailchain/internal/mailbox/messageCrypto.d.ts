import { EncryptedContent } from '@mailchain/crypto';
import { KeyRing } from '@mailchain/keyring';
import { Payload } from '../transport/payload/payload.js';
import '../transport/payload/headers.js';

/**
 * Used for encryption and decryption of the full message content.
 */
type MessageCrypto = {
    encrypt: (payload: Payload) => Promise<EncryptedContent>;
    decrypt: (content: EncryptedContent) => Promise<Payload>;
};
declare function createMailchainMessageCrypto(keyRing: KeyRing): MessageCrypto;

export { MessageCrypto, createMailchainMessageCrypto };
