import { PublicKey } from '@mailchain/crypto';
import { KeyRing } from '@mailchain/keyring';
import { MailData } from '../transport/mail/types.js';

type MessageIdCreator = (params: {
    mailData: MailData;
    type: 'sent';
} | {
    mailData: MailData;
    type: 'received';
    mailbox: PublicKey;
    owner: string;
}) => Promise<string>;
declare function createMailchainMessageIdCreator(keyRing: KeyRing): MessageIdCreator;

export { MessageIdCreator, createMailchainMessageIdCreator };
