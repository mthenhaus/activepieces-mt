import { ED25519ExtendedPrivateKey } from '@mailchain/crypto';
import { PayloadHeaders } from './headers.js';

/**
 * Payload.
 */
interface Payload<H extends PayloadHeaders = PayloadHeaders> {
    /**
     * @see defaults to {@link PayloadHeaders}
     */
    Headers: H;
    /**
     * Raw/data/object that are decrypted and parsed base on the the headers.
     */
    Content: Buffer;
}
declare function serializeAndEncryptPayload(payload: Payload, payloadRootEncryptionKey: ED25519ExtendedPrivateKey): Promise<Buffer>;

export { Payload, serializeAndEncryptPayload };
