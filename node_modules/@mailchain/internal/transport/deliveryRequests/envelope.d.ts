import { PublicKey, ExtendedPrivateKey, RandomFunction } from '@mailchain/crypto';
import { p as protocol } from '../../protocol.d-0f2c60f5.js';
import 'protobufjs';

/**
 *
 * @param recipientIdentityKey this is a Mailchain identity key.
 * @param messageKey root key used to encrypt message and payload
 */
declare function createEnvelope(recipientMessagingKey: PublicKey, messageRootEncryptionKey: ExtendedPrivateKey, messageURI: string, rand?: RandomFunction): Promise<protocol.Envelope>;

export { createEnvelope };
