import { PublicKey, ExtendedPrivateKey, RandomFunction } from '@mailchain/crypto';
import { p as protocol } from '../../protocol.d-0f2c60f5.js';
import 'protobufjs';

declare function createDelivery(recipientMessagingKey: PublicKey, messageRootEncryptionKey: ExtendedPrivateKey, messageURI: string, rand?: RandomFunction): Promise<protocol.Delivery>;

export { createDelivery };
