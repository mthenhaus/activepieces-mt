import { PresentationPayload, VerifiableCredential } from 'did-jwt-vc';
import { DecentralizedIdentifier, MailchainDecentralizedIdentifier } from './did.js';
import '@mailchain/addressing';

type CreatePresentationPayloadParams = {
    requestId: string;
    verifiableCredential: VerifiableCredential;
    verifier: DecentralizedIdentifier;
    issuanceDate: Date;
    /**
     * The date and time that the credential will expire.
     */
    expirationDate?: Date;
    holder: MailchainDecentralizedIdentifier;
};
/**
 * Create presentation payload that is then signed to create a verified presentation.
 * @param params
 * @returns
 */
declare function createPresentationPayload(params: CreatePresentationPayloadParams): PresentationPayload;

export { createPresentationPayload };
