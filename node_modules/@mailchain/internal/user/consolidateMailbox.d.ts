import { NewUserMailbox } from './userProfile.js';
import '@mailchain/crypto';
import '@mailchain/api';
import '../configuration.js';
import './migrations.js';
import '../identityKeys/identityKeys.js';
import '@mailchain/addressing';
import '../nameservices/nameservices.js';
import './types.js';
import 'protobufjs';
import '../migration.js';

declare function consolidateMailbox(mailbox: NewUserMailbox): NewUserMailbox;
declare function consolidateMailboxLabel(mailbox: NewUserMailbox): NewUserMailbox;
/**
 * Consolidate the provided mailbox aliases with the following rules:
 * - There can be only single alias per `address`. If there are duplicates, only the first one in the entries is kept, the others are ignored.
 */
declare function consolidateMailboxAliases(mailbox: NewUserMailbox): NewUserMailbox;

export { consolidateMailbox, consolidateMailboxAliases, consolidateMailboxLabel };
