import { ProtocolType } from '@mailchain/addressing';
import { PublicKey } from '@mailchain/crypto';
import { MailData } from '../transport/mail/types.js';

type ParseMimeTextResult = {
    mailData: MailData;
    addressIdentityKeys: Map<string, {
        identityKey: PublicKey;
        protocol: ProtocolType;
        network?: string;
    }>;
};
declare function parseMimeText(content: Buffer): Promise<ParseMimeTextResult>;

export { ParseMimeTextResult, parseMimeText };
