import { PublicKey, PrivateKey } from '@mailchain/crypto';
import { PublicKey as PublicKey$1, PrivateKey as PrivateKey$1 } from '../api/api.js';
import 'axios';
import '../api/configuration.js';
import '../api/base.js';

/** Convert {@link PublicKey} to {@link ApiPublicKey}. */
declare function convertPublic(key: PublicKey): PublicKey$1;
/** Convert {@link PrivateKey} to {@link ApiPrivateKey} */
declare function convertPrivate(key: PrivateKey): PrivateKey$1;

export { convertPrivate, convertPublic };
