import { PublicKey as PublicKey$1, PrivateKey as PrivateKey$1 } from '@mailchain/crypto';
import { PublicKey, PrivateKey } from '../api/api.js';
import 'axios';
import '../api/configuration.js';
import '../api/base.js';

/** Convert {@link ApiPublicKey} to {@link PublicKey} */
declare function convertPublic(key: PublicKey): PublicKey$1;
/** Convert {@link ApiPrivateKey} to {@link PrivateKey} */
declare function convertPrivate(key: PrivateKey): PrivateKey$1;

export { convertPrivate, convertPublic };
