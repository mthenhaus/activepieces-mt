type TokenPayload = {
    /** The HTTP method */
    m: string;
    /** pathname */
    url: string;
    /** The length of the data payload of the request  */
    len?: number;
    /** hash of the body */
    bodyHash?: string;
    /** host */
    aud: string;
    /** query params */
    q?: string;
};
declare function createTokenPayload(url: URL, method: string, data: unknown, expires: number): TokenPayload;

export { TokenPayload, createTokenPayload };
