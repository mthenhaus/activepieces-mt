import { PublicKey } from '../public.js';

declare function kindFromPublicKey(key: PublicKey): string;
declare function publicKeyFromKind(kind: string, data: Uint8Array): PublicKey;

export { kindFromPublicKey, publicKeyFromKind };
