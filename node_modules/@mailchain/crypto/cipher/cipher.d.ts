/**
 * NaCl with Elliptic Curve Diffie–Hellman key exchange
 */
declare const NACLECDH = 42;
/**
 * NaCl with a secret key
 */
declare const NACLSK = 43;
declare const KindNaClSecretKey = "nacl-secret-key";
type EncryptedContent = Uint8Array;
type PlainContent = Uint8Array;
interface Decrypter {
    decrypt: (input: EncryptedContent) => Promise<PlainContent>;
}
interface Encrypter {
    encrypt: (input: PlainContent) => Promise<EncryptedContent>;
}

export { Decrypter, EncryptedContent, Encrypter, KindNaClSecretKey, NACLECDH, NACLSK, PlainContent };
