import { Decrypter, EncryptedContent, PlainContent } from '../cipher.js';
import { PrivateKey } from '../../private.js';
import '../../public.js';

declare class PrivateKeyDecrypter implements Decrypter {
    private _secretKey;
    private _keyId;
    constructor(privateKey: PrivateKey);
    static fromPrivateKey(key: PrivateKey): PrivateKeyDecrypter;
    decrypt(input: EncryptedContent): Promise<PlainContent>;
}

export { PrivateKeyDecrypter };
