import { PublicKey } from '../../public.js';
import { EncryptedContent } from '../cipher.js';

declare function serializePublicKeyEncryptedContent(sealedBox: EncryptedContent, pubKey: PublicKey): EncryptedContent;
declare function serializePrivateKeyEncryptedContent(sealedBox: EncryptedContent, keyId: number): EncryptedContent;
declare function deserializePublicKeyEncryptedContent(input: Uint8Array): {
    encryptedContent: EncryptedContent;
    pubKey: PublicKey;
};
declare function deserializePrivateKeyEncryptedContent(input: Uint8Array): {
    encryptedContent: EncryptedContent;
    keyId: number;
};

export { deserializePrivateKeyEncryptedContent, deserializePublicKeyEncryptedContent, serializePrivateKeyEncryptedContent, serializePublicKeyEncryptedContent };
