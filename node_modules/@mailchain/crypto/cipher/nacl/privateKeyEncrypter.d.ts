import { Encrypter, EncryptedContent } from '../cipher.js';
import { RandomFunction } from '../../rand.js';
import { PrivateKey } from '../../private.js';
import '@noble/hashes/utils';
import '../../public.js';

declare class PrivateKeyEncrypter implements Encrypter {
    private _rand;
    private _secretKey;
    private _keyId;
    constructor(privateKey: PrivateKey, rand?: RandomFunction);
    static fromPrivateKey(key: PrivateKey, rand?: RandomFunction): PrivateKeyEncrypter;
    encrypt(input: Uint8Array): Promise<EncryptedContent>;
}

export { PrivateKeyEncrypter };
