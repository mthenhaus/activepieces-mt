import { Decrypter, EncryptedContent, PlainContent } from '../cipher.js';
import { KeyExchange } from '../keyExchange.js';
import { PrivateKey } from '../../private.js';
import '../../public.js';

declare class PublicKeyDecrypter implements Decrypter {
    private _keyEx;
    private _prvKey;
    constructor(keyEx: KeyExchange, prvKey: PrivateKey);
    static FromPrivateKey(key: PrivateKey): PublicKeyDecrypter;
    decrypt(input: EncryptedContent): Promise<PlainContent>;
}

export { PublicKeyDecrypter };
