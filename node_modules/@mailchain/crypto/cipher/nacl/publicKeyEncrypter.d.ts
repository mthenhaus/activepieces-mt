import { KeyExchange } from '../keyExchange.js';
import { Encrypter, EncryptedContent } from '../cipher.js';
import { RandomFunction } from '../../rand.js';
import { PublicKey } from '../../public.js';
import '../../private.js';
import '@noble/hashes/utils';

declare class PublicKeyEncrypter implements Encrypter {
    private _keyEx;
    private _pubKey;
    private _rand;
    constructor(keyEx: KeyExchange, pubKey: PublicKey, rand?: RandomFunction);
    static FromPublicKey(key: PublicKey): PublicKeyEncrypter;
    encrypt(input: Uint8Array): Promise<EncryptedContent>;
}

export { PublicKeyEncrypter };
