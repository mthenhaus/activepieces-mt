import { PublicKey } from '../public.js';

declare const SECP256R1PublicKeyLength = 33;
declare class SECP256R1PublicKey implements PublicKey {
    readonly bytes: Uint8Array;
    readonly curve: string;
    constructor(bytes: Uint8Array);
    verify(message: Uint8Array, sig: Uint8Array): Promise<boolean>;
}

export { SECP256R1PublicKey, SECP256R1PublicKeyLength };
