import { PublicKey } from '../public.js';
import { PrivateKey } from '../private.js';

declare const PrivateKeyLen = 32;
declare class UnknownPrivateKey implements PrivateKey {
    bytes: Uint8Array;
    publicKey: PublicKey;
    curve: string;
    constructor(bytes?: Uint8Array);
    sign(_message: Uint8Array): Promise<Uint8Array>;
}

export { PrivateKeyLen, UnknownPrivateKey };
