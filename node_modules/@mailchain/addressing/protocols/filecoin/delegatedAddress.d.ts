import { PublicKey } from '@mailchain/crypto';

declare const MAINNET_PREFIX = "f";
declare const CALIBRATION_PREFIX = "t";
declare function convertFilDelegatedAddressToEthAddress(fileCoinAddress: string): {
    data: Uint8Array;
    error?: undefined;
} | {
    error: Error;
    data?: undefined;
};
/**
 * Compute the Filecoin F4 Ethereum delegated address from the provided PublicKey.
 */
declare function filDelegatedAddressFromPublicKey(publicKey: PublicKey): Promise<Uint8Array>;
declare function convertEthAddressToFilDelegated(ethereumAddress: Uint8Array, prefix?: typeof MAINNET_PREFIX | typeof CALIBRATION_PREFIX): Uint8Array;

export { CALIBRATION_PREFIX, MAINNET_PREFIX, convertEthAddressToFilDelegated, convertFilDelegatedAddressToEthAddress, filDelegatedAddressFromPublicKey };
