import { PublicKey } from '@mailchain/crypto';

declare function validateSolanaAddress(addressID: string): boolean;
declare function solanaAddressFromPublicKey(publicKey: PublicKey): Uint8Array;

export { solanaAddressFromPublicKey, validateSolanaAddress };
