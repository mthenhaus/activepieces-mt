import { NameServiceAddress } from './nameServiceAddress.js';

/**
 * Compare two addresses for their equality.
 * @returns true if the addresses are equal, false if not.
 */
declare function isSameAddress(a: string | NameServiceAddress, b: string | NameServiceAddress): boolean;

export { isSameAddress };
