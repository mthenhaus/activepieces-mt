import { NameServiceAddress } from './nameServiceAddress.js';
import { ProtocolType } from './protocols/consts.js';

/**
 * Helper method for creating of {@link MailchainAddress} for when the address is with specific protocol.
 * It provides the correct casing for the username.
 */
declare function createWalletAddress(username: string, protocol: ProtocolType, mailchainAddressDomain: string): NameServiceAddress;

export { createWalletAddress };
