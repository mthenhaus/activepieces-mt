import {
  FILECOIN_PREFIX_F4_ETHEREUM,
  FILECOIN_PREFIX_T4_ETHEREUM
} from "./chunk-SSBZ77W5.mjs";
import {
  convertFilDelegatedAddressToEthAddress
} from "./chunk-BSPEQE4E.mjs";

// src/protocols/filecoin/address.ts
import { isBase32 } from "@mailchain/encoding";
function validateFilecoinAddress(address) {
  if (address.length < 42 && address.length > 49) {
    return false;
  } else if (!address.startsWith(FILECOIN_PREFIX_F4_ETHEREUM) && !address.startsWith(FILECOIN_PREFIX_T4_ETHEREUM)) {
    return false;
  } else if (!isBase32(address.slice(4))) {
    return false;
  }
  return convertFilDelegatedAddressToEthAddress(address).error === void 0;
}

export {
  validateFilecoinAddress
};
