import { SignerWithPublicKey, PublicKey, EncryptedContent, PlainContent, Encrypter, Decrypter, PrivateKey } from '@mailchain/crypto';

interface KeyRingDecrypter extends SignerWithPublicKey {
    ecdhDecrypt(bundleEphemeralKey: PublicKey, input: EncryptedContent): Promise<PlainContent>;
}
type InboxKey = Encrypter & Decrypter;
declare function ecdhKeyRingDecrypter(privateKey: PrivateKey): KeyRingDecrypter;

export { InboxKey, KeyRingDecrypter, ecdhKeyRingDecrypter };
