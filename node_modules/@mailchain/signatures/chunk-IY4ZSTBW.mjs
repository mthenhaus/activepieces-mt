import {
  signRawEd25519,
  verifyRawEd25519
} from "./chunk-RNDAINMT.mjs";

// src/mailchain_username.ts
var mailchainUsernameMessage = function(message) {
  const prefix = Buffer.from(`Mailchain username ownership:
${message.length}
`, "utf-8");
  return Buffer.concat([prefix, message]);
};
async function signMailchainUsername(signer, username) {
  return signRawEd25519(signer, mailchainUsernameMessage(username));
}
async function verifyMailchainUsername(key, signature, username) {
  return verifyRawEd25519(key, mailchainUsernameMessage(username), signature);
}

export {
  signMailchainUsername,
  verifyMailchainUsername
};
