import { EncodingType } from '@mailchain/encoding';
import { ProtocolType } from '@mailchain/addressing';

interface ProofParams {
    AddressEncoding: EncodingType;
    PublicKeyEncoding: EncodingType;
    Locale: string;
    Variant: string;
}
declare function getLatestProofParams(protocol: ProtocolType, locale: string): ProofParams;
declare function getMailchainUsernameParams(): ProofParams;

export { ProofParams, getLatestProofParams, getMailchainUsernameParams };
