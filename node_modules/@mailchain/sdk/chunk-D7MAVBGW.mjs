// src/internal/addressNonce.ts
import { defaultConfiguration } from "@mailchain/internal/configuration";
import { AddressNonce } from "@mailchain/internal/messagingKeys";
async function getMessagingKeyLatestNonce(address, protocol) {
  return AddressNonce.create(defaultConfiguration).getMessagingKeyLatestNonce(address, protocol);
}

export {
  getMessagingKeyLatestNonce
};
