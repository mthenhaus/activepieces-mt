// src/internal/resolvers.ts
import { MessagingKeys } from "@mailchain/internal/messagingKeys";
import { defaultConfiguration } from "@mailchain/internal/configuration";
async function resolveAddress(address, config = defaultConfiguration) {
  return MessagingKeys.create(config).resolve(address);
}

export {
  resolveAddress
};
